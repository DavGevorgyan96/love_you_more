# API: use /api for same-origin (recommended). Avoid full preview URLs to prevent DEPLOYMENT_NOT_FOUND.
VITE_API_URL=/api
CONTACT_EMAIL=you@example.com

# Show only Coming Soon page (set to true for production until launch)
VITE_SHOW_COMING_SOON=true
